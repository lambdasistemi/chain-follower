# chain-follower

Abstract chain follower types — `Follower`, `Intersector`, `ProgressOrRewind` — parameterized over point, slot, and payload types.

## Build

```bash
nix develop -c just build
```

## License

Apache-2.0
